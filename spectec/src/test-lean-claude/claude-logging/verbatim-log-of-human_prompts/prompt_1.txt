Understand SpecTec by reading the documentation and code in this codebase. Understand the specifications for WASM 2.0 in the specifications folder written using SpecTec EL, both technically as well as the context and intuition behind the specification.

I've created a new folder `spectec/src/test-lean-claude` with 2 files in it; one of them is the Lean backend's output of the 2.0 EL spec, and the other is a library file that the first needs for the `derive_deceq` command. There is also an empty folder `spectec/src/test-lean-claude/claude-logging`. Your task is to translate as much of `https://github.com/Wasm-DSL/spectec/tree/rocq-backend-proof/spectec/test-rocq/theories` as possible into Lean. Your translation should be directly lemma-for-lemma / definition-for-definition etc if possible, unless there are obvious optimizations that can be made. For proofs, due to proof irrelevance you can solve them however you want; it might not be optimal to directly copy the Rocq tactics into Lean, but the important thing is that the signatures must be directly from the Rocq proof.

First, read and understand the entirely of the Rocq proof. It is incomplete, but you should be able to understand its goals. In the Rocq proof, wasm.v is the equivalent of wasm2.0.lean, and there is no equivalent to ExtendedDeriveDecEq.lean and `spectec/src/test-lean-claude/claude-logging`, which is ok.

You can find some previous attempts in the `test-lean` folder; in there (amongst a lot of jump and scrap) you will find a handwritten `typing_inversion.lean` and `custom_notation.lean` that themselves should mimic the rocq proof almost lemma by lemma (though file organization might differ). Also useful might be the test-lean/test-lean-claude folder; this is a previous attempt by a different claude session, and contains much logging.

You should not directly try and imitate the file organization of previous attempts since they both differ from the Rocq organizatio; for example there is no `Counterexample.lean` equivalent in the Rocq proof.

As you go, you should log your progress or doubts in `spectec/src/test-lean-claude/claude-logging` in a structured and modular way. You should log things separately for humans to read (more succinct and intuitive summaries), and for a subsequent Claude session to read (more verbose and precise).

You are expected to keep running and will probably run out of usage time / context window, which is why you should log things for subsequent Claude sessions (which possibly are starting with 0 context) to take over smoothly.

You should start by sketching the entire structure of the Rocq proof, making sure to get signatures right and filling out details with `sorry`s, and making sure everything typechecks, then move on to actually fill in easier supporting infrastructure like lemmas, proofs and defs, then finally if there is time remaining, try to complete the harder proofs.

You should NEVER modify anything outside of `spectec/src/test-lean-claude`. In fact, you should run a periodic programmatic check to verify that you have not done so, and log the check output in the claude-logging directory. If you create any agents or otherwise call a different LLM / Claude session, you MUST make it keenly aware of this safety limitation, and it must do the same checks and log it neatly in the claude-logging folder.

There is also a partial Isabelle proof at https://github.com/Wasm-DSL/spectec/tree/isabelle-mech-backend/spectec/isabelle_type_safety_proof (backend output at https://github.com/Wasm-DSL/spectec/blob/isabelle-mech-backend/spectec/isabelle_type_safety_proof/isabelle_reference_output_wasm2.thy) which you can reference, but since Rocq is closer to Lean in nature you should prioritize that. In any case, your end result should mirror the Rocq proof in top-level signatures and structure, not the Isabelle one (if they differ).

Please ask any questions as you continue to progress or encounter any important design decisions / problems / if any information I gave you seems to be inaccurate. I may also give you additional instructions or interrupt you as you progress.

Note that not all the `*_is_wf` theorems in `wasm2.0.lean` are necessarily true; I will have a separate Claude agent looking at the `*_is_wf` theorems to suss out which are true and which are not, or I might do it manually. This will happen in parallel to your work, so don't be surprised if the wasm2.0.lean file changes suddenly. If you do have to use a `*_is_wf` theorem that is still `sorry`, you can either try and solve it yourself if it's simple, or else treat it as true for now. Either way, you should log your actions regarding `*_is_wf` theorems in a separate section in the claude-logging folder. In particular, you should create a list of `*_is_wf` theorems you can needed to use, and whether they are true, false, or unknown (treated as true for now).